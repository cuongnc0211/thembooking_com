<% content_for :body_class, "bg-[#0A0A0A] text-slate-100" %>
<% content_for :nav_class, "bg-white/5 backdrop-blur-md" %>
<% content_for :footer_override do %><% end %>

<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8 relative overflow-hidden">
  <!-- Aurora background -->
  <div class="absolute inset-0 aurora-bg pointer-events-none"></div>
  <!-- Dot grid overlay -->
  <div class="absolute inset-0 pointer-events-none opacity-10" style="background-image: radial-gradient(circle, rgba(255,255,255,0.2) 1px, transparent 1px); background-size: 32px 32px;"></div>

  <div class="relative z-10 w-full max-w-md animate-fade-in-up">
    <!-- Brand -->
    <div class="text-center mb-8">
      <%= link_to root_path do %>
        <span class="text-2xl font-display font-bold gradient-text-hero">ThemBooking</span>
      <% end %>
    </div>

    <!-- Card -->
    <div class="dark-glass-card p-8 space-y-6">
      <!-- Header -->
      <div class="text-center">
        <h1 class="text-2xl font-display font-bold text-white mb-1"><%= t('views.sessions.new.title') %></h1>
        <p class="text-slate-400 text-sm"><%= t('views.sessions.new.subtitle') %></p>
      </div>

      <!-- Form -->
      <%= form_with url: session_url, class: "space-y-5" do |form| %>
        <!-- Email Field -->
        <div>
          <%= form.label :email_address, t('activerecord.attributes.user.email_address'), class: "form-label-dark" %>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207"/>
              </svg>
            </div>
            <%= form.email_field :email_address,
                required: true,
                autofocus: true,
                autocomplete: "username",
                placeholder: t('views.sessions.new.email_placeholder'),
                value: params[:email_address],
                class: "form-input-dark pl-10" %>
          </div>
        </div>

        <!-- Password Field -->
        <div>
          <div class="flex items-center justify-between mb-1.5">
            <%= form.label :password, t('activerecord.attributes.user.password'), class: "form-label-dark mb-0" %>
            <%= link_to t('views.sessions.new.forgot_password'), new_password_path, class: "text-xs text-primary-400 hover:text-primary-300 transition-colors" %>
          </div>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <svg class="h-5 w-5 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
              </svg>
            </div>
            <%= form.password_field :password,
                required: true,
                autocomplete: "current-password",
                placeholder: t('views.sessions.new.password_placeholder'),
                maxlength: 72,
                class: "form-input-dark pl-10" %>
          </div>
        </div>

        <!-- Submit Button -->
        <%= form.submit t('views.sessions.new.submit'), class: "btn-primary w-full cursor-pointer mt-2" %>
      <% end %>

      <!-- Divider -->
      <div class="relative">
        <div class="absolute inset-0 flex items-center">
          <div class="w-full border-t border-white/10"></div>
        </div>
        <div class="relative flex justify-center text-xs">
          <span class="px-4 bg-transparent text-slate-500"><%= t('views.sessions.new.no_account') %></span>
        </div>
      </div>

      <!-- Sign Up Link -->
      <%= link_to t('views.sessions.new.sign_up_link'), new_registration_path,
          class: "flex items-center justify-center w-full px-6 py-3 rounded-xl font-medium border border-white/15 text-slate-300 hover:bg-white/5 hover:border-white/25 transition-all duration-200" %>
    </div>
  </div>
</div>
