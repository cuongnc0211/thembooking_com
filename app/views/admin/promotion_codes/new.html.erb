<% admin_breadcrumbs(link_to("Promotion Codes", admin_promotion_codes_path), "New") %>

<div class="mb-6">
  <h1 class="text-2xl font-bold text-gray-900">New Promotion Code</h1>
</div>

<div class="bg-white rounded-xl border border-gray-200 p-6 max-w-2xl">
  <%= render "form", promotion_code: @promotion_code %>
</div>
