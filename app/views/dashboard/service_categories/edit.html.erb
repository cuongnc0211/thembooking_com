<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold"><%= t('views.dashboard.service_categories.edit.title') %></h1>
    <%= link_to "← Back to Categories", dashboard_branch_service_categories_path(@branch), class: "text-sm text-slate-500 hover:text-slate-700" %>
  </div>
  <div class="card p-6">
    <%= render "form", category: @category, assignable_services: @assignable_services %>
  </div>
</div>
